[pytest]
# Hermetic by default: network-dependent tests are marked @pytest.mark.online and
# skipped unless you explicitly opt in with:  pytest -m online
addopts = -m "not online"
markers =
    online: tests that require a live internet connection (deselected by default)
